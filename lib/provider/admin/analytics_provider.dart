import 'package:flutter/material.dart';

class AnalyticsProvider extends ChangeNotifier {
  // Deprecated provider. 
  // Analytics logic has been moved to DatabaseService streams directly in UI.
  // This file is kept to prevent breakage of any remaining imports.
}
