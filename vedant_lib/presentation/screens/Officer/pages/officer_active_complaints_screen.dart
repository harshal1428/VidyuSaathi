import 'package:flutter/material.dart';
import 'officer_all_tickets_screen.dart'; // Reuse existing logic filtered by active

class OfficerActiveComplaintsScreen extends OfficerAllTicketsScreen {
  const OfficerActiveComplaintsScreen({Key? key}) : super(key: key);
  
  // Inheritance used here for simplicity as it shares same structure but would filter data in real app
}
